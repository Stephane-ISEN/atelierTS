version: "3.9"

services:
  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.22.0
    container_name: atelierts-mlflow
    restart: unless-stopped

    ports:
      - "5000:5000"

    volumes:
      - ./mlflow:/mlflow

    command: >
      mlflow server
      --host 0.0.0.0
      --port 5000
      --backend-store-uri sqlite:///mlflow/mlflow.db
      --default-artifact-root file:///home/isen/atelierTS/mlflow/artifacts

